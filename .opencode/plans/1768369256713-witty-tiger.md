# Plan: Create AGENTS.md File

## Overview
Create an AGENTS.md file to guide AI coding agents working in this Vite + TypeScript portfolio website codebase.

## Analysis Summary

### Project Details
- **Type**: Vite + TypeScript portfolio website
- **Package Manager**: npm (package-lock.json present)
- **Language**: TypeScript, CSS, HTML5
- **Testing**: None configured
- **Linting**: None configured
- **Formatting**: None configured

### Build/Dev Commands
- `npm run dev` - Start development server
- `npm run build` - TypeScript compile + Vite build
- `npm run preview` - Preview production build
- Type check: `npx tsc` (integrated into build)

### Code Style Conventions Identified

**Naming:**
- Variables/Functions: camelCase with action verbs (e.g., `initPortfolioFilter()`, `navMenu`)
- Types/Interfaces: PascalCase (e.g., `ObserverOptions`)
- CSS Classes: kebab-case (e.g., `.portfolio-item`, `.service-card`)
- Files: camelCase (.ts), kebab-case (.css), kebab-case for assets
- Constants: Mostly camelCase (inconsistent), prefer SCREAMING_SNAKE_CASE for truly global constants

**Formatting:**
- Indentation: 4 spaces
- Type annotations: Explicit (e.g., `: void`, `: string`)
- Spaces after colons in types, commas in parameters, around operators

**Patterns:**
- JSDoc comments for functions
- Visual section separators: `// ===================================`
- Try-catch-finally for async error handling
- Early returns for validation
- Explicit type assertions and guards
- Arrow functions for event handlers
- Cleanup functions with beforeunload handler
- ARIA attributes for accessibility
- Performance: RequestAnimationFrame, debouncing

**TypeScript Configuration:**
- Strict mode enabled
- noUnusedLocals, noUnusedParameters
- noFallthroughCasesInSwitch
- Bundler module resolution

## Implementation Plan

### 1. Create AGENTS.md
**File**: `/Users/aravinthan/Developer/personal/blendedbyvish/AGENTS.md`

**Structure:**
1. Project Overview (tech stack, purpose)
2. Commands (dev, build, type-check)
3. Testing Note (none configured)
4. Code Style Guidelines
   - Naming conventions
   - Formatting (4 spaces)
   - TypeScript conventions
   - Patterns to follow
5. File Structure Conventions
6. Best Practices (accessibility, performance, error handling)

**Target Length**: ~150 lines

### 2. Content to Include

From tsconfig.json:
- Strict mode requirements
- No unused locals/parameters

From main.ts:
- camelCase functions with action verbs
- JSDoc documentation
- Section separators
- Try-catch-finally patterns
- Cleanup patterns
- Accessibility (ARIA)

From style.css:
- CSS custom properties organization
- kebab-case class names
- Section headers

### 3. Key Guidelines to Document
- Always use explicit type annotations
- Document functions with JSDoc
- Use section separators for major code blocks
- Implement cleanup patterns
- Add ARIA attributes
- Use TypeScript strict mode
- No test command available (warn agents)
- No linting configured

## Critical Files
- `/Users/aravinthan/Developer/personal/blendedbyvish/AGENTS.md` (to create)
- `/Users/aravinthan/Developer/personal/blendedbyvish/package.json` (reference for scripts)
- `/Users/aravinthan/Developer/personal/blendedbyvish/tsconfig.json` (reference for TS config)
- `/Users/aravinthan/Developer/personal/blendedbyvish/src/main.ts` (reference for style)

## Verification
After creating AGENTS.md, verify:
1. File exists at correct path
2. Content is accurate based on actual project setup
3. Length is approximately 150 lines
4. All build/dev commands are correct
5. Code style guidelines match existing code patterns

No existing rules files to incorporate (no AGENTS.md, .cursor/rules, .cursorrules, or .github/copilot-instructions.md found).
