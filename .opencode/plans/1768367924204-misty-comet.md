# Code Review & Business Features Implementation Plan

## Overview
Perform final Severity 1 code review focusing on Safari iOS compatibility, add portfolio filtering system, serverless contact form, broken image handling, and TypeScript strictness improvements.

## Implementation Steps

### 1. Safari iOS Stress Test Fixes
**Files:** `src/main.ts`, `src/style.css`
- Add dynamic viewport height calculation in TypeScript to fix 100vh bug on older iOS
- Update testimonials carousel CSS to respect native swipe back gestures

### 2. Portfolio Filtering System
**Files:** `src/main.ts`, `src/style.css`
- Implement TypeScript logic for filter buttons with smooth animations
- Add CSS styles for active filter states and transitions

### 3. Serverless Contact Form
**Files:** `src/main.ts`, `src/style.css`
- Add form submission handling with Web3Forms API
- Implement loading states and validation feedback
- Add CSS for form input validation states

### 4. Broken Images Handler
**Files:** `src/main.ts`
- Add generic onerror handler for all images
- Replace broken images with CSS gradient placeholder

### 5. TypeScript Strictness Audit
**Files:** `src/main.ts`
- Fix unsafe `as HTMLElement` casts with proper null checks
- Wrap critical DOM queries in try/catch or type guards

## Critical Files to Modify
- `/Users/aravinthan/Developer/personal/blendedbyvish/src/main.ts` - Add all TS logic and fixes
- `/Users/aravinthan/Developer/personal/blendedbyvish/src/style.css` - Add filter and form styles

## Verification Steps
1. Run `npm run lint` to ensure code quality
2. Run `npm run typecheck` to verify TypeScript strictness
3. Test mobile menu on iOS Safari (viewport height fix)
4. Test portfolio filters functionality
5. Test contact form submission (replace API key)
6. Test image onerror by temporarily breaking an image src
7. Verify no console errors on page load</content>
<parameter name="filePath">/Users/aravinthan/Developer/personal/blendedbyvish/.opencode/plans/1768367924204-misty-comet.md